<div class='comment'>
  <p class='comment-body'><%= comment.body %></p>

  <% if allow? 'comments', 'edit', comment: comment %>
    <%= link_to edit_comment_path(comment), remote: true, 'data-id' => comment.id do %>
      <i class='icon-pencil'></i> Edit
    <% end %>
  <% end %>
</div>
